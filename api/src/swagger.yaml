openapi: 3.0.3
info:
  title: Budget-Flow API
  description: API de gestão financeira compartilhada
  version: 1.0.0
servers:
  - url: http://localhost:3001
    description: Servidor local

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Token:
      type: object
      properties:
        token:
          type: string
    Transaction:
      type: object
      properties:
        id:
          type: integer
        descricao:
          type: string
        valor:
          type: number
        tipo:
          type: string
          enum: [ENTRADA, SAIDA]
        categoria:
          type: string
          enum:
            - Aluguel/Financiamento
            - Condominio
            - Luz (Energia)
            - Agua
            - Internet/TV a cabo
            - Gas
            - Supermercado (Compra do mes)
            - Restaurantes e Bares
            - Delivery (iFood/Ze Delivery)
            - Padaria e Cafes
            - Combustivel
            - Uber / 99 / Taxi
            - Transporte Publico (Onibus/Metro)
            - Manutencao e Seguro (Gastos fixos com carro)
            - Farmacia
            - Plano de Saude / Medicos
            - Academia
            - Higiene e Cosmeticos
            - Assinaturas (Netflix, Spotify, Games)
            - Roupas e Acessorios
            - Hobbies / Cinema / Shows
            - Presentes
            - Mensalidades (Escola/Faculdade)
            - Cursos e Livros
            - Fatura do Cartao
            - Emprestimos / Juros
            - Investimentos / Poupanca
            - Impostos (IPTU, IPVA)
            - Outros
        responsavel:
          type: string
        data:
          type: string
          format: date
        user_id:
          type: integer
    TransactionInput:
      type: object
      required: [descricao, valor, tipo, categoria, responsavel, data]
      properties:
        descricao:
          type: string
        valor:
          type: number
          minimum: 0.01
          maximum: 1000000000
          description: Valor da transação. Deve estar entre 0.01 e 1.000.000.000
        tipo:
          type: string
          enum: [ENTRADA, SAIDA]
        categoria:
          type: string
          enum:
            - Aluguel/Financiamento
            - Condominio
            - Luz (Energia)
            - Agua
            - Internet/TV a cabo
            - Gas
            - Supermercado (Compra do mes)
            - Restaurantes e Bares
            - Delivery (iFood/Ze Delivery)
            - Padaria e Cafes
            - Combustivel
            - Uber / 99 / Taxi
            - Transporte Publico (Onibus/Metro)
            - Manutencao e Seguro (Gastos fixos com carro)
            - Farmacia
            - Plano de Saude / Medicos
            - Academia
            - Higiene e Cosmeticos
            - Assinaturas (Netflix, Spotify, Games)
            - Roupas e Acessorios
            - Hobbies / Cinema / Shows
            - Presentes
            - Mensalidades (Escola/Faculdade)
            - Cursos e Livros
            - Fatura do Cartao
            - Emprestimos / Juros
            - Investimentos / Poupanca
            - Impostos (IPTU, IPVA)
            - Outros
        responsavel:
          type: string
        data:
          type: string
          format: date
    Summary:
      type: object
      properties:
        total_entradas:
          type: number
        total_despesas:
          type: number
        saldo_mensal:
          type: number
        is_limite_excedido:
          type: boolean
        resumo_por_pessoa:
          type: object
          additionalProperties:
            type: number
        resumo_por_pessoa_categoria:
          type: object
          additionalProperties:
            type: object
            additionalProperties:
              type: number
    CategoryGroup:
      type: object
      properties:
        group:
          type: string
        options:
          type: array
          items:
            type: string
    User:
      type: object
      properties:
        id:
          type: integer
        username:
          type: string
        active:
          type: boolean
    UserCreateInput:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
    UserPasswordUpdateInput:
      type: object
      required: [current_password, new_password]
      properties:
        current_password:
          type: string
        new_password:
          type: string
    UserStatusUpdateInput:
      type: object
      required: [active]
      properties:
        active:
          type: boolean
    UserForgotPasswordInput:
      type: object
      required: [username]
      properties:
        username:
          type: string
    UserResetPasswordInput:
      type: object
      required: [username, reset_token, new_password]
      properties:
        username:
          type: string
        reset_token:
          type: string
        new_password:
          type: string
    ResetTokenResponse:
      type: object
      properties:
        reset_token:
          type: string
        expires_in_seconds:
          type: integer
    Error:
      type: object
      properties:
        erro:
          type: string

security:
  - bearerAuth: []

paths:
  /auth/login:
    post:
      tags: [Auth]
      summary: Autenticar usuário
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username:
                  type: string
                password:
                  type: string
      responses:
        '200':
          description: Login realizado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Token'
        '401':
          description: Credenciais inválidas
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users:
    get:
      tags: [Users]
      summary: Listar usuários cadastrados
      responses:
        '200':
          description: Lista de usuários
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags: [Users]
      summary: Cadastrar novo usuário
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserCreateInput'
      responses:
        '201':
          description: Usuário criado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Erro de validação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Usuário já cadastrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/{id}:
    get:
      tags: [Users]
      summary: Buscar usuário por id
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Usuário encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Usuário não encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/forgot-password:
    post:
      tags: [Users]
      summary: Solicitar token de recuperação de senha
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserForgotPasswordInput'
      responses:
        '200':
          description: Token de recuperação gerado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResetTokenResponse'
        '400':
          description: Erro de validação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Usuário não encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/reset-password:
    post:
      tags: [Users]
      summary: Redefinir senha com token de recuperação
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserResetPasswordInput'
      responses:
        '200':
          description: Senha redefinida
          content:
            application/json:
              schema:
                type: object
                properties:
                  mensagem:
                    type: string
        '400':
          description: Erro de validação ou token inválido
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Usuário não encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/password:
    patch:
      tags: [Users]
      summary: Atualizar senha do usuário autenticado
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserPasswordUpdateInput'
      responses:
        '200':
          description: Senha atualizada
          content:
            application/json:
              schema:
                type: object
                properties:
                  mensagem:
                    type: string
        '400':
          description: Erro de validação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Usuário não encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /users/{id}/status:
    patch:
      tags: [Users]
      summary: Ativar ou desativar usuário (cadastro lógico)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserStatusUpdateInput'
      responses:
        '200':
          description: Status atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
        '400':
          description: Erro de validação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Usuário não encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /transactions:
    get:
      tags: [Transactions]
      summary: Listar transações por mês e ano
      parameters:
        - in: query
          name: mes
          required: true
          schema:
            type: integer
        - in: query
          name: ano
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Lista de transações
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Transaction'
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags: [Transactions]
      summary: Criar nova transação
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionInput'
      responses:
        '201':
          description: Transação criada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          description: Erro de validação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /transactions/{id}:
    put:
      tags: [Transactions]
      summary: Atualizar transação
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransactionInput'
      responses:
        '200':
          description: Transação atualizada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          description: Erro de validação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Transação não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      tags: [Transactions]
      summary: Excluir transação
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        '204':
          description: Transação excluída
        '401':
          description: Não autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Transação não encontrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /summary/{mes}/{ano}:
    get:
      tags: [Summary]
      summary: Resumo financeiro mensal
      parameters:
        - in: path
          name: mes
          required: true
          schema:
            type: integer
        - in: path
          name: ano
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: Resumo mensal
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Summary'

  /categories:
    get:
      tags: [Categories]
      summary: Listar categorias permitidas para transacoes
      responses:
        '200':
          description: Lista de grupos e categorias
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CategoryGroup'
        '401':
          description: Nao autorizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
